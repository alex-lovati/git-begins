pour cloner :
git clone (<nom du repository>)

le trois commande de push:
git add .
git commit -m (<nom du commit>)
git push

ajout: git pull

aller plus loin

creer ue branch: git branch(<nom de la branch>)
changer de branch: switch (<nom de la branch>)